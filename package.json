{
  "name": "wild-wild-horses",
  "version": "1.0.0",
  "description": "Static site built with 11ty, managed via Pages CMS, deployed to GitHub Pages",
  "type": "module",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve --watch"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^6.0.4",
    "js-yaml": "^4.1.1"
  }
}
